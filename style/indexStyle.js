import { StyleSheet } from 'react-native';

export const indexStyle = StyleSheet.create({
    total_container: {
        backgroundColor: '#1e1e1e',
        color: '#d4d4d4',
        flexGrow: 1,
        alignItems: "center"
    },
});